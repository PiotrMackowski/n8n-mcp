# Pin all future npm install --save to exact versions (no ^ or ~)
save-exact=true

# Require lockfile to exist and be up-to-date
package-lock=true

# Fail on peer dependency conflicts instead of silently ignoring
strict-peer-deps=false

# Use lockfile for reproducible installs
prefer-offline=true
